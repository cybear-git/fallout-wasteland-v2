```sql
-- This project contains only SQL database files (.sql)
-- No compiled artifacts, dependencies, or temporary files to ignore
-- Source code files (.sql) should be tracked in version control
```